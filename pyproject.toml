[project]
name = "score-prep"
version = "0.1.0"
description = "Random measure practice for recital preparation"
requires-python = ">=3.12"
dependencies = [
    "pyfiglet>=1.0",
    "textual>=3.0",
]

[project.scripts]
score-prep = "score_prep:ScorePrep.run"
